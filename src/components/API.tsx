import React, { useState, useEffect } from 'react';

const apiKey = import.meta.env.VITE_API_KEY

type component = {
    sendWord: (v: string) => void;
}

export default function Api({sendWord} : component) {
    

    return(
        <>
        </>
    )
};